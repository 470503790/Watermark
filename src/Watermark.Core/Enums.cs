namespace Watermark.Core;

public enum BlendMode { Normal, Multiply, Screen, Overlay, Darken, Lighten }
public enum Anchor { TL, TC, TR, CL, CC, CR, BL, BC, BR }
public enum ImageSizing { None, FitWidth, FitHeight, Contain, Cover }
